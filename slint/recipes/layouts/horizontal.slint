import { HorizontalBox, Button } from "std-widgets.slint";
export component HorizontalRecipe inherits Window {
    HorizontalBox {
        Button { text: "First"; }
        Button { text: "Second"; }
        Button { text: "Third"; }
    }
}