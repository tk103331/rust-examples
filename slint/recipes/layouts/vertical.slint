import { VerticalBox, Button } from "std-widgets.slint";
export component VerticalRecipe inherits Window {
    VerticalBox {
        Button { text: "First"; }
        Button { text: "Second"; }
        Button { text: "Third"; }
    }
}